import React from 'react';
import Modal from '../Modal';

const StreamDelete = () => {
  return (
    <div>
      Stream delete
      <Modal name="Delete" />
    </div>
  );
};
export default StreamDelete;
